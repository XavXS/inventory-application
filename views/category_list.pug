extends layout

block content
  a(href='/create') create category
  p #{title}:
  if category_list.length
    ul
        each category in category_list
            li
                a(href=category.url) #{category.name}
  else
    p categories not found